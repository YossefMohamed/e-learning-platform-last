/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["localhost", "e-learning-platform-server.onrender.com"],
  },
};

module.exports = nextConfig;
